services:
  frontend:
    image: vnq88/teacher-ai-dashboard-frontend:1.0.0
    container_name: teacher-ai-frontend
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    external: true
