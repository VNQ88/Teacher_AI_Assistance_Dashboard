services:
  frontend:
    image: vnq88/teacher-ai-dashboard-frontend:latest
    container_name: teacher-ai-frontend
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    external: true
